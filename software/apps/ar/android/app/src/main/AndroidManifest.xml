<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Real camera-passthrough AR, built on the native ARCore SDK
             instead of the WebView (see NativeArActivity's class doc for
             why the WebView path is a structural dead end). This is now the
             app's actual entry point: opening the app goes straight into
             the camera view, matching how Pokemon Go / IKEA Place open. -->
        <activity
            android:name=".ar.NativeArActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NativeAr"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- ARCore: "required" triggers Google Play Services for AR to
             install/update automatically. Without this the WebXR
             immersive-ar session can start but never actually receive
             camera frames, which is what was producing a black view
             instead of real camera passthrough. -->
        <meta-data android:name="com.google.ar.core" android:value="required" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.ar" android:required="true" />
</manifest>
