// Intentionally minimal: the frontend talks to the backend directly over
// WebSocket/REST (ws://localhost:8000, http://localhost:8000), the same way
// it does in a browser, so no privileged IPC bridge is required here.
